---
description: Enforces the correct project name (AgriCrate).
---
# Project Naming Conventions

- The project's name is **AgriCrate**.
- Do **NOT** use the legacy name "AgriCrate".
- When generating copy, documentation, or code comments, always use "AgriCrate".
- For internal identifiers (like directory paths or old repository names that cannot be easily changed), respect the system layout, but all user-facing text, classes, and documentation must use the new brand name.
